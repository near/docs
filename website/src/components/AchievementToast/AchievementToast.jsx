import React from 'react';

// This component is kept for compatibility but won't be used
// since react-hot-toast handles the rendering directly
const AchievementToast = ({ achievement, isVisible, onClose, queueLength }) => {
  // This component is no longer needed with react-hot-toast
  // but kept for backward compatibility
  return null;
};

export default AchievementToast;
